pipeline {
  agent any
  stages {
    stage('per-build') {
      steps {
        echo 'progress'
      }
    }

    stage('build') {
      steps {
        echo 'build'
      }
    }

    stage('deploy') {
      steps {
        echo 'deploy'
      }
    }

  }
}